#pragma once
/*
#include <GL/freeglut.h>
#include "camera.hpp"

void OnMouseClick(Camera &camera, int button, int state, int x, int y)
{
    if (state == GLUT_DOWN)
    {
        camera.MouseDown(x, y);
    }
}

void OnMouseMove(Camera &camera, int x, int y)
{
    camera.MouseMove(x, y);
}

// keyboard events WASD
void OnKeyClick(Camera &camera, unsigned char key, int x, int y)
{
    int type = -1;
    if (key == 'w')
    {
        type = 0;
    }
    else if (key == 'a')
    {
        type = 1;
    }
    else if (key == 's')
    {
        type = 2;
    }
    else if (key == 'd')
    {
        type = 3;
    }
    camera.KeyboardMove(type);
}
 */